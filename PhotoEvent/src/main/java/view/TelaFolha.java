package view;

import javax.swing.*;
import java.awt.*;

public class TelaFolha  extends JPanel
{
    public TelaFolha()
    {
        initComponents();
    }
    @SuppressWarnings("unchecked")
    private void initComponents()
    {
        setLayout(null);
        setBackground(new Color(255, 255, 255));
    }
}